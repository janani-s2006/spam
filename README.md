# Spam Classifier

A Python-based spam detection project using machine learning models (SVC, etc.) on spam.tsv dataset.

## Files
- `Spam6.py`: Main classification script.
- `spam.tsv`: Dataset.
- Model files (*.pkl): Trained models.

## Usage
Run `python Spam6.py` to classify spam.

